{
    IBClasses = (
        {CLASS = FirstResponder; LANGUAGE = ObjC; SUPERCLASS = NSObject; }, 
        {
            CLASS = GCPreferences; 
            LANGUAGE = ObjC; 
            OUTLETS = {
                mAdvancedView = NSView; 
                mDetectionView = NSView; 
                mGeneralView = NSView; 
                mNumberView = NSView; 
                mUpdateView = NSView; 
            }; 
            SUPERCLASS = NSWindowController; 
        }
    ); 
    IBVersion = 1; 
}